<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.helpinghands">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.HelpingHands"
        tools:replace="android:icon">
        <activity
            android:name=".Activity.DonorHome"
            android:label="@string/title_activity_donor_home"
            android:theme="@style/Theme.HelpingHands.NoActionBar"></activity>
        <activity
            android:name=".Activity.App_Drawer"
            android:label="@string/title_activity_app__drawer"
            android:screenOrientation="portrait"
            android:theme="@style/Theme.HelpingHands.NoActionBar" />
        <activity
            android:name=".Activity.SplashActivity"
            android:screenOrientation="portrait"
            android:theme="@style/SplashTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activity.MainActivity"
            android:screenOrientation="portrait" />
    </application>

</manifest>